<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Form Pilote</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/form.css">
    <link rel="icon" type="image/png" sizes="32x32" href="/public/img/logo.png">
    <link rel="stylesheet" href="/public/css/navbar.css">
</head>

<body>

    <%- include('../layouts/header.ejs') %>
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-6 illus">
                    <img id="human" src="/public/img/create.png" alt="">
                </div>
                <div class="col-lg-6 col-sm-12 registration-form">
                    <form class="needs-validation" method="POST" novalidate>
                        <div class="titre">
                            <h1>Mettre à jour un Pilote</h1>
                        </div>
                        <div class="form-group mb-2">
                            <label class="mb-0 pl-2 text-muted" for="nomp">Nom</label>
                            <input type="text" class="form-control item mb-0" id="nomp" name="nomp"
                                value="<%= pilotes.Nom %>" placeholder="Nom du pilote" required>
                        </div>
                        <div class="form-group mb-2">
                            <label class="mb-0 pl-2 text-muted" for="prenomp">Prénom</label>
                            <input type="text" class="form-control item mb-0" id="prenomp" name="prenomp"
                                value="<%= pilotes.Prenom %>" placeholder="Prénom du pilote" required>
                        </div>
                        <div class="form-group mb-2">
                            <label class="mb-0 pl-2 text-muted" for="emailp">Adresse E-mail</label>
                            <input type="text" class="form-control item mb-0" id="emailp" name="emailp"
                                value="<%= pilotes.Email %>" placeholder="E-mail du pilote" required>
                        </div>
                        <div class="form-group mb-2">
                            <label class="mb-0 pl-2 text-muted" for="usernamep">Nom d'utilisateur</label>
                            <input type="text" class="form-control item mb-0" id="usernamep" name="usernamep"
                                value="<%= pilotes.Username %>" placeholder="Username">
                        </div>
                        <div class="form-group mb-2">
                            <label class="mb-0 pl-2 text-muted" for="passwordp">Mot de passe</label>
                            <input type="password" class="form-control item mb-0" id="passwordp" name="passwordp"
                                value="<%= pilotes.Password %>" placeholder="Password" required>
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-block create-company">Valider les
                                modifications</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <%- include ('../layouts/footer.ejs') %>

            <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
            <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
</body>

</html>